import React from 'react';
import './Breaks.css';

function Breaks() {
  return (
    <div className="breaks">
      {/* Breaks list and add break button */}
    </div>
  );
}

export default Breaks;
