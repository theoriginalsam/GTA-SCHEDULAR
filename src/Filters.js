import React from 'react';
import './Filters.css';

function Filters() {
  return (
    <div className="filters">
      {/* Filters for course status, term, etc. */}
    </div>
  );
}

export default Filters;
